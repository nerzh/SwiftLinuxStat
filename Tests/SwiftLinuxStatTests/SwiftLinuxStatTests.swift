import XCTest
@testable import SwiftLinuxStat

final class SwiftLinuxStatTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(swift_linux_stat().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
